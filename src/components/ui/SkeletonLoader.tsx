import React from 'react';\nimport { motion } from 'framer-motion';\n\ninterface SkeletonLoaderProps {\n  variant?: 'card' | 'list' | 'player' | 'waveform' | 'text' | 'avatar' | 'custom';\n  className?: string;\n  lines?: number;\n  height?: string;\n  width?: string;\n  animated?: boolean;\n}\n\nconst SkeletonLoader: React.FC<SkeletonLoaderProps> = ({\n  variant = 'card',\n  className = '',\n  lines = 3,\n  height = 'auto',\n  width = 'auto',\n  animated = true\n}) => {\n  const shimmerAnimation = {\n    backgroundPosition: ['-200% 0', '200% 0'],\n  };\n\n  const shimmerTransition = {\n    duration: 2,\n    repeat: Infinity,\n    ease: \"easeInOut\"\n  };\n\n  const baseClasses = `bg-gradient-to-r from-white/5 via-white/15 to-white/5 bg-[length:200%_100%] rounded-lg ${animated ? 'animate-pulse' : ''}`;\n\n  if (variant === 'card') {\n    return (\n      <motion.div \n        className={`p-6 space-y-4 ${className}`}\n        initial={{ opacity: 0 }}\n        animate={{ opacity: 1 }}\n        transition={{ duration: 0.3 }}\n      >\n        {/* Header with avatar and title */}\n        <div className=\"flex items-center space-x-4\">\n          <motion.div \n            className={`w-12 h-12 rounded-xl ${baseClasses}`}\n            animate={animated ? shimmerAnimation : {}}\n            transition={animated ? shimmerTransition : {}}\n          />\n          <div className=\"flex-1 space-y-2\">\n            <motion.div \n              className={`h-4 w-3/4 ${baseClasses}`}\n              animate={animated ? shimmerAnimation : {}}\n              transition={animated ? shimmerTransition : {}}\n            />\n            <motion.div \n              className={`h-3 w-1/2 ${baseClasses}`}\n              animate={animated ? shimmerAnimation : {}}\n              transition={animated ? shimmerTransition : {}}\n            />\n          </div>\n        </div>\n        \n        {/* Content lines */}\n        <div className=\"space-y-2\">\n          {[...Array(lines)].map((_, i) => (\n            <motion.div\n              key={i}\n              className={`h-3 ${i === lines - 1 ? 'w-2/3' : 'w-full'} ${baseClasses}`}\n              animate={animated ? shimmerAnimation : {}}\n              transition={animated ? { ...shimmerTransition, delay: i * 0.1 } : {}}\n            />\n          ))}\n        </div>\n        \n        {/* Action buttons */}\n        <div className=\"flex items-center space-x-2 pt-2\">\n          <motion.div \n            className={`w-20 h-8 rounded-lg ${baseClasses}`}\n            animate={animated ? shimmerAnimation : {}}\n            transition={animated ? shimmerTransition : {}}\n          />\n          <motion.div \n            className={`w-16 h-8 rounded-lg ${baseClasses}`}\n            animate={animated ? shimmerAnimation : {}}\n            transition={animated ? shimmerTransition : {}}\n          />\n        </div>\n      </motion.div>\n    );\n  }\n\n  if (variant === 'list') {\n    return (\n      <motion.div \n        className={`space-y-3 ${className}`}\n        initial={{ opacity: 0 }}\n        animate={{ opacity: 1 }}\n        transition={{ duration: 0.3 }}\n      >\n        {[...Array(lines)].map((_, i) => (\n          <div key={i} className=\"flex items-center space-x-3\">\n            <motion.div \n              className={`w-10 h-10 rounded-lg ${baseClasses}`}\n              animate={animated ? shimmerAnimation : {}}\n              transition={animated ? { ...shimmerTransition, delay: i * 0.1 } : {}}\n            />\n            <div className=\"flex-1 space-y-1\">\n              <motion.div \n                className={`h-3 w-3/4 ${baseClasses}`}\n                animate={animated ? shimmerAnimation : {}}\n                transition={animated ? { ...shimmerTransition, delay: i * 0.1 + 0.05 } : {}}\n              />\n              <motion.div \n                className={`h-2 w-1/2 ${baseClasses}`}\n                animate={animated ? shimmerAnimation : {}}\n                transition={animated ? { ...shimmerTransition, delay: i * 0.1 + 0.1 } : {}}\n              />\n            </div>\n            <motion.div \n              className={`w-8 h-8 rounded-full ${baseClasses}`}\n              animate={animated ? shimmerAnimation : {}}\n              transition={animated ? { ...shimmerTransition, delay: i * 0.1 + 0.15 } : {}}\n            />\n          </div>\n        ))}\n      </motion.div>\n    );\n  }\n\n  if (variant === 'player') {\n    return (\n      <motion.div \n        className={`p-6 space-y-6 ${className}`}\n        initial={{ opacity: 0, y: 20 }}\n        animate={{ opacity: 1, y: 0 }}\n        transition={{ duration: 0.4 }}\n      >\n        {/* Header */}\n        <div className=\"flex items-center justify-between\">\n          <motion.div \n            className={`h-4 w-24 ${baseClasses}`}\n            animate={animated ? shimmerAnimation : {}}\n            transition={animated ? shimmerTransition : {}}\n          />\n          <motion.div \n            className={`w-6 h-6 rounded-full ${baseClasses}`}\n            animate={animated ? shimmerAnimation : {}}\n            transition={animated ? shimmerTransition : {}}\n          />\n        </div>\n        \n        {/* Track info and artwork */}\n        <div className=\"flex items-start space-x-6\">\n          <motion.div \n            className={`w-24 h-24 rounded-2xl ${baseClasses}`}\n            animate={animated ? shimmerAnimation : {}}\n            transition={animated ? shimmerTransition : {}}\n          />\n          <div className=\"flex-1 space-y-3\">\n            <motion.div \n              className={`h-5 w-4/5 ${baseClasses}`}\n              animate={animated ? shimmerAnimation : {}}\n              transition={animated ? shimmerTransition : {}}\n            />\n            <motion.div \n              className={`h-3 w-2/3 ${baseClasses}`}\n              animate={animated ? shimmerAnimation : {}}\n              transition={animated ? shimmerTransition : {}}\n            />\n            <div className=\"flex items-center space-x-2\">\n              {[...Array(3)].map((_, i) => (\n                <motion.div\n                  key={i}\n                  className={`w-6 h-6 rounded-full ${baseClasses}`}\n                  animate={animated ? shimmerAnimation : {}}\n                  transition={animated ? { ...shimmerTransition, delay: i * 0.1 } : {}}\n                />\n              ))}\n            </div>\n          </div>\n        </div>\n        \n        {/* Waveform */}\n        <motion.div \n          className={`h-20 w-full rounded-xl ${baseClasses}`}\n          animate={animated ? shimmerAnimation : {}}\n          transition={animated ? shimmerTransition : {}}\n        />\n        \n        {/* Progress bar */}\n        <div className=\"space-y-2\">\n          <div className=\"flex justify-between text-sm\">\n            <motion.div \n              className={`h-3 w-8 ${baseClasses}`}\n              animate={animated ? shimmerAnimation : {}}\n              transition={animated ? shimmerTransition : {}}\n            />\n            <motion.div \n              className={`h-3 w-8 ${baseClasses}`}\n              animate={animated ? shimmerAnimation : {}}\n              transition={animated ? shimmerTransition : {}}\n            />\n          </div>\n          <motion.div \n            className={`h-2 w-full rounded-full ${baseClasses}`}\n            animate={animated ? shimmerAnimation : {}}\n            transition={animated ? shimmerTransition : {}}\n          />\n        </div>\n        \n        {/* Controls */}\n        <div className=\"flex items-center justify-between\">\n          <div className=\"flex items-center space-x-3\">\n            {[...Array(2)].map((_, i) => (\n              <motion.div\n                key={i}\n                className={`w-10 h-10 rounded-full ${baseClasses}`}\n                animate={animated ? shimmerAnimation : {}}\n                transition={animated ? { ...shimmerTransition, delay: i * 0.1 } : {}}\n              />\n            ))}\n          </div>\n          \n          <motion.div \n            className={`w-16 h-16 rounded-full ${baseClasses}`}\n            animate={animated ? shimmerAnimation : {}}\n            transition={animated ? shimmerTransition : {}}\n          />\n          \n          <div className=\"flex items-center space-x-3\">\n            {[...Array(2)].map((_, i) => (\n              <motion.div\n                key={i}\n                className={`w-10 h-10 rounded-full ${baseClasses}`}\n                animate={animated ? shimmerAnimation : {}}\n                transition={animated ? { ...shimmerTransition, delay: i * 0.1 + 0.2 } : {}}\n              />\n            ))}\n          </div>\n        </div>\n        \n        {/* Volume control */}\n        <div className=\"flex items-center justify-center space-x-3\">\n          <motion.div \n            className={`w-6 h-6 rounded-full ${baseClasses}`}\n            animate={animated ? shimmerAnimation : {}}\n            transition={animated ? shimmerTransition : {}}\n          />\n          <motion.div \n            className={`w-32 h-1 rounded-full ${baseClasses}`}\n            animate={animated ? shimmerAnimation : {}}\n            transition={animated ? shimmerTransition : {}}\n          />\n          <motion.div \n            className={`w-6 h-3 ${baseClasses}`}\n            animate={animated ? shimmerAnimation : {}}\n            transition={animated ? shimmerTransition : {}}\n          />\n        </div>\n      </motion.div>\n    );\n  }\n\n  if (variant === 'waveform') {\n    return (\n      <motion.div \n        className={`flex items-end justify-center space-x-1 h-20 ${className}`}\n        initial={{ opacity: 0 }}\n        animate={{ opacity: 1 }}\n        transition={{ duration: 0.3 }}\n      >\n        {[...Array(40)].map((_, i) => (\n          <motion.div\n            key={i}\n            className={`w-1 bg-gradient-to-t from-white/10 to-white/30 rounded-full`}\n            style={{ height: `${Math.random() * 60 + 20}%` }}\n            animate={animated ? {\n              height: [`${Math.random() * 60 + 20}%`, `${Math.random() * 80 + 10}%`, `${Math.random() * 60 + 20}%`]\n            } : {}}\n            transition={animated ? {\n              duration: 2,\n              repeat: Infinity,\n              delay: i * 0.05,\n              ease: \"easeInOut\"\n            } : {}}\n          />\n        ))}\n      </motion.div>\n    );\n  }\n\n  if (variant === 'text') {\n    return (\n      <motion.div \n        className={`space-y-2 ${className}`}\n        initial={{ opacity: 0 }}\n        animate={{ opacity: 1 }}\n        transition={{ duration: 0.3 }}\n      >\n        {[...Array(lines)].map((_, i) => (\n          <motion.div\n            key={i}\n            className={`h-3 ${i === lines - 1 ? 'w-2/3' : 'w-full'} ${baseClasses}`}\n            animate={animated ? shimmerAnimation : {}}\n            transition={animated ? { ...shimmerTransition, delay: i * 0.1 } : {}}\n          />\n        ))}\n      </motion.div>\n    );\n  }\n\n  if (variant === 'avatar') {\n    return (\n      <motion.div \n        className={`w-10 h-10 rounded-full ${baseClasses} ${className}`}\n        style={{ width, height }}\n        animate={animated ? shimmerAnimation : {}}\n        transition={animated ? shimmerTransition : {}}\n        initial={{ opacity: 0 }}\n      />\n    );\n  }\n\n  if (variant === 'custom') {\n    return (\n      <motion.div \n        className={`${baseClasses} ${className}`}\n        style={{ width, height }}\n        animate={animated ? shimmerAnimation : {}}\n        transition={animated ? shimmerTransition : {}}\n        initial={{ opacity: 0 }}\n      />\n    );\n  }\n\n  return null;\n};\n\nexport default SkeletonLoader;